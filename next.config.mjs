/** @type {import('next').NextConfig} */
const nextConfig = {
    // Required for next-intl
    // For pages dir
    // i18n: {
    //   locales: ["en", "tr"],
    //   defaultLocale: "en",
    // },
};

export default nextConfig;
